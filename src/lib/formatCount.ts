/**
 * Server-side compact counts (channel subscriber text, etc.).
 * Implementation lives in shared/format so client and server stay aligned.
 */
export { formatCompactNumber as formatCompactCount } from '../shared/format'
